export default class TodoList {
    constructor(id, title, todoListId, createdAt){
        this.id = id;
        this.title = title;
        this.todoListId = todoListId;
        this.createdAt = createdAt;
    }
}
